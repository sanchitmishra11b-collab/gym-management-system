web: python manage.py migrate && python manage.py shell -c "from django.contrib.auth.models import User; from djangogym1.models import AdminProfile; u = User.objects.get(username='sanchitmishra'); u.set_password('sanchit'); u.save(); AdminProfile.objects.get_or_create(user=u, defaults={'is_approved': True})" && gunicorn djangogym.wsgi --log-file -